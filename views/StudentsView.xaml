<Page
    x:Class="App2.StudentsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Custom="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <CommandBar Background="Transparent" IsOpen="False" DefaultLabelPosition="Right">
            <AppBarButton Icon="Add" Label="Ajouter"/>
            <AppBarButton Icon="Upload" Label="Exporter" />
            <AppBarButton Icon="Download" Label="Importer" />
        </CommandBar>
        <Custom:DataGrid ItemsSource="{x:Bind ListCustomers}"  HorizontalAlignment="Left" Height="564" Margin="52,100,0,0" VerticalAlignment="Top" Width="Auto"/>
        <TextBlock HorizontalAlignment="Left" Margin="52,11,0,0" Text="Liste des étudiants" TextWrapping="Wrap" VerticalAlignment="Top" SelectionChanged="TextBlock_SelectionChanged" FontSize="14" FontWeight="Bold"/>
    </Grid>
</Page>
