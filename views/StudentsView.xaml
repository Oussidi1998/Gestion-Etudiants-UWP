<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Custom="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:Custom1="using:Telerik.UI.Xaml.Controls.Grid"
    x:Class="App2.StudentsView"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Loaded="win_loaded">
        <CommandBar Background="Transparent" IsOpen="False" DefaultLabelPosition="Right">
            <AppBarButton Icon="Add" Label="Ajouter"/>
            <AppBarButton Icon="Upload" Label="Exporter" />
            <AppBarButton Icon="Download" Label="Importer" />
        </CommandBar>
        <TextBlock HorizontalAlignment="Left" Margin="52,11,0,0" Text="Liste des étudiants" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="20" FontWeight="Bold"/>
        <Custom1:RadDataGrid ItemsSource="{x:Bind Path=ListStudents}"  HorizontalAlignment="Left" Margin="52,100,0,0" VerticalAlignment="Top" Width="auto" Height="564"/>
    </Grid>
</Page>
